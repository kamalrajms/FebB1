23/02/2026

## react intro

    What is React?

    1) React is a JavaScript library (not a framework) developed by Facebook (Meta) for building user interfaces (UIs), especially Single Page Applications (SPAs).

    2) It helps developers build reusable UI components that update efficiently when data changes.

    Instead of reloading a whole webpage when something changes, React updates only the parts of the UI that need to change.

    -------
    Why do we need React?

	It makes UI development faster, cleaner, and more efficient.

	Provides a better user experience with smooth updates without page reloads.


    ## must need to install node.js
 
## procedure to initiate project

    step 1 : npm create vite@latest [project_name]
    step 2 : select react
    step 3 : select javascript+swc
    step 4 : npm install                // install node module packages
    step 5 : npm run dev

    if you want to re-run your project mean 
        npm run dev


24/02/2026

## Props

    props is nothing but drilling data from parent to child components.

        declearing a props
        destructuring a props

## Conditional rendering

    Conditional rendering means showing or hiding  parts of UI 
    based on some conditions (ststs,props..).

        ternary operator {condition}?true:false
        logical operator    && || !

## list rendering

    rendering multiple items dynamically using .map()

25/02/2026

## styleing in react.js

    global way styleing--- index.css,app.css are called as global way style
    external way of styleing--- external styleing file for a component
    module way styleing  ---  we need to save the name as [filename.module.css]
    inline styleing--- style
    object way styleing---- adding properites as an object

## useState()and Forms and Controlled inputs

    useState is a react hook that allows you to add state (data that can change over time)
    to a functionl component.

    Normally ,in js functions, variables disappear after the function runs.
    But with useState ,react remembers values between re-renders.

    syntax:

        const[state,setState]=useState(initialValue)

        state-- the current value of the variables
        setState--- a function that update the state
        initialValue--- the starting value of the state.

27/02/2026

## useEffect()

    In react,useEffect is a react hook that lets you perform side effect in functionl component.
    Side effect are tsk that happends outside the normal react rendering process ,like...

        Fetch data from an API
        Updating the browser DOM manually
        Starting /stopping timers

    syntax:

        useEffect(()=>{

            //code logic

        },[dependency 1,dependency 2.....])
        

        dependency--- if that dependency is  empty means ,useEffect 
                        willbe only once at the begining.

3/3/2026

## useContext()

    Passing data from a parent component down to deep nested hild component is annoying.
    Normally ,you'd have to pass Props manually Throug each level (Child  props drilling).
    useContext lets you skip all the middle component and share data directly.


        create a contect
        Provide a value
        Consume the value
